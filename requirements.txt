# Runtime dependencies verified against Python 3.13 on 2026-08-25.
# Keep these versions synchronized with pyproject.toml.
azure-identity==1.25.3
azure-keyvault-secrets==4.11.2
azure-storage-blob==12.30.0
psycopg[binary]==3.3.4
python-dotenv==1.2.3
requests==2.34.2
